import { CouterBtnDirective } from './couter-btn.directive';

describe('CouterBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new CouterBtnDirective();
    expect(directive).toBeTruthy();
  });
});
